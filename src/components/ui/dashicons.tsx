// Dashboard nav icons — lucide, one stroke/size. LayoutHome aliases
// LayoutDashboard so the sidebar "overview" item reads clearly.
export {
  Users, CalendarClock, CreditCard, Layers, BarChart3, Settings, LogOut,
  LayoutDashboard as LayoutHome,
} from 'lucide-react';

export const ICON = { size: 18, strokeWidth: 1.5 } as const;
